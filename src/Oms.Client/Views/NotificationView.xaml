<UserControl x:Class="Oms.Client.Views.NotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Border BorderBrush="#FFFFBE00" BorderThickness="0 5 0 0">
        <Border BorderThickness="1" BorderBrush="#FFFFBE00" Padding="3" Background="#FF3E3E42">
            <DockPanel DataContext="{Binding Notification}" Width="200" Height="40">
                <TextBlock Text="{Binding Title}" DockPanel.Dock="Top" FontWeight="Bold" Foreground="White" />
                <TextBlock Text="{Binding Description}" Foreground="White"/>
            </DockPanel>
        </Border>
    </Border>
</UserControl>
